using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GowFirebaseExample : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // GameOfWhales.Instance.UpdateToken(token, GameOfWhales.PROVIDER_FCM);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
